# Copy this to .env and fill in values. Never commit .env to git.

# --- OpenAI ---
OPENAI_API_KEY=sk-...

# Optional: override model (default gpt-4.1)
# HERMITCLAW_MODEL=gpt-4o

# --- Cloudflare Tunnel ---
# Get from: Zero Trust → Networks → Tunnels → your tunnel → Configure → Install and run connector
# Use the "token" value from the cloudflared command shown there.
CLOUDFLARED_TUNNEL_TOKEN=eyJ...

# --- GHCR (for watchtower auto-updates) ---
# GitHub username
GHCR_USER=tjlong84
# GitHub Personal Access Token with read:packages scope
# Create at: https://github.com/settings/tokens → Fine-grained → Repository → Packages: Read
GHCR_TOKEN=ghp_...
